# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/
ENV/

# Distribution / env
.env
.venv
devenv.local.nix

# Virtualenv
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# VS Code
.vscode/

# Models (Keep large tasks out if possible, but user might want them. 
# hand_landmarker.task is ~8MB, usually okay for GitHub, but let's exclude standard big binary data if it's easily downloadable.)
# However, for a "ready to run" repo, 8MB is fine.

# Local logs
*.log
